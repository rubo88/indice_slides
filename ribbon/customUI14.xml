<customUI xmlns="http://schemas.microsoft.com/office/2009/07/customui">
  <ribbon>
    <tabs>
      <tab id="tabSectionNav" label="Section Nav">
        <group id="grpNavBar" label="Navigation Bar">
          <button id="btnCreate"
                  label="Create Nav Bar"
                  onAction="RibbonCreateNavBar"
                  imageMso="ShapesInsertShape"
                  size="large"
                  screentip="Create Section Navigation Bar"
                  supertip="Adds a clickable navigation bar at the top of every slide based on the presentation sections. The current section is highlighted. Re-running updates the bar if sections changed." />
          <button id="btnRemove"
                  label="Remove Nav Bar"
                  onAction="RibbonRemoveNavBar"
                  imageMso="Delete"
                  size="large"
                  screentip="Remove Section Navigation Bar"
                  supertip="Removes all navigation bar shapes from every slide in the active presentation." />
        </group>
      </tab>
    </tabs>
  </ribbon>
</customUI>
